name: 'Action Auditor'
description: 'Detect exposed API Keys.'
inputs:
  github_token:
    description: "Get GitHub Token"
    required: false
    default: ${{ github.token }}
runs:
  using: "composite"
  steps:
    - id: actionprint
      run: docker images
      shell: bash
